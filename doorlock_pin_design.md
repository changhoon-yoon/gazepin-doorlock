# GazePIN — 시선 좌/우 입력 도어락 비밀번호 프로토콜 설계

> 검증 시뮬레이션: `pin_protocol_sim.py` (2026-08-04 실행 결과 반영)
> 입력 계층 검증: `lr_gaze_fast.py` — 좌/우 판별 20/20 (100%), 평균 반응 0.91초

## 1. 핵심 논리: "2중1택 = 부실한 암호"가 오해인 이유

**입력 채널의 폭과 비밀(암호)의 크기는 별개다.**
비밀은 여전히 4자리 PIN(10,000가지)이고, 좌/우 선택은 그 비밀을 1비트씩
직렬 전송하는 통신 채널일 뿐이다. 숫자당 log₂10 ≈ 3.32비트를 4회의 이진
선택으로 전송한다. 키패드는 같은 3.32비트를 한 번의 키 입력으로 전송하지만
**그 입력이 관찰자에게 그대로 노출**되고, 우리 채널은 노출되지 않는다.

| | 키패드 | 패턴락 | 얼굴인식 단독 | **GazePIN** |
|---|---|---|---|---|
| 비밀 공간 | 10⁴ | ~10³·⁵ | 없음 | **10⁴ (동일)** |
| 어깨너머 관찰 1회 | 전부 유출 | 전부 유출 | — | **0비트 (실측)** |
| 스머지/자국 | 취약 | 취약 | — | 없음 |
| 사진/영상 스푸핑 | — | — | 취약 | **재생 무력 (세션 랜덤)** |

## 2. 프로토콜 (숫자 1개당 4라운드, 4자리 = 16선택)

1. 화면에 0–9 전체를 **좌 그룹 5개 / 우 그룹 5개**로 나눠 표시한다.
   분할 규칙: 현재 "셀"(지금까지 사용자의 응답 패턴이 같은 숫자 집합)마다
   최대한 절반으로 나누고, 어느 쪽에 놓을지는 난수로 정한다.
   → 분할은 **공개 정보(이전 분할)와 난수만으로 생성**되며 비밀과 무관.
2. 사용자는 자기 숫자가 있는 쪽을 **시선 좌/우**로 답한다
   (진입 임계 초과 → 이벤트 1회 소비 → 중앙 복귀 확인 후 다음 라운드).
3. 4라운드 후 응답열(4비트)이 숫자 하나를 유일하게 결정한다
   (셀 크기 10→5→3→2→1). 화면에는 후보 축소 과정을 절대 표시하지 않는다
   — 매 라운드 10개 전부, 무작위 배치로 표시.
4. 4자리 입력 완료 후에만 일괄 판정. 실패 시 어느 자리가 틀렸는지
   알려주지 않는다. 5회 연속 실패 → 5분 잠금 + 경보(선택).

## 3. 보안 성질 (시뮬레이션 20만 회 실측)

| 공격자 | 관찰 가능 | 결과 |
|---|---|---|
| 눈만 관찰 (현실적 어깨너머) | 좌/우 응답열 | **상호정보량 0.00055비트 ≈ 추정오차 수준(0.00049). 최적 추측 10.49% (우연=10%)** |
| 화면만 관찰 | 분할 시퀀스 | 분할 생성이 비밀을 입력받지 않음(구조적 독립). 추측 9.97% |
| 눈+화면 동시 녹화 | 전부 | PIN 복원 가능 — 키패드가 카메라 1대에 뚫리는 것과 동급. **완화: 시야각 필터(privacy film)를 화면에 부착하면 측면에서 분할 자체를 못 읽음** + 잠금 정책 |
| 무차별 대입 | — | 5회 잠금 기준 창당 0.05% (키패드와 동일) |
| 사용자 영상 재생(딥페이크 포함) | — | 새 세션의 분할은 새 난수 → 재생 영상의 응답은 무작위와 같음 → 1/10⁴. **얼굴인식 락과 달리 스푸핑이 구조적으로 무력** |

추가 성질:
- **오류 자기감지**: 16가지 응답열 중 유효한 것은 10개. 선택 실수의 상당수는
  즉시 "무효 응답열"로 감지되어 그 자리만 재입력 (실측: 오류율 1%에서
  PIN 성공률 91.3%, 자리 재시도 평균 0.07회).
- **습관 유출 없음**: 배치가 매번 바뀌므로 사용자에게 공간적 패턴이 생기지 않음.
- 입력 시간: 16선택 × ~1.4초(반응 0.91초 실측 + 중앙 복귀) ≈ **23초/4자리**.

## 4. 학술 근거 (발표 인용용)

- Roth, Richter, Freidinger, *A PIN-entry method resilient against shoulder
  surfing*, **ACM CCS 2004** — 숫자를 흑/백 두 그룹으로 나눠 소속 그룹만
  응답하는 "cognitive trapdoor" PIN 입력. 본 설계는 이 검증된 구조의
  응답 채널을 버튼에서 시선으로 대체한 것.
- Kumar, Garfinkel, Boneh, Winograd, *Reducing shoulder-surfing by using
  gaze-based password entry*, **SOUPS 2007** (EyePassword, Stanford) —
  시선 입력이 어깨너머 공격을 실질 차단하며 속도·오류율이 키보드와
  대등함을 사용자 실험으로 입증.
- 차별점: 두 선례의 결합 + 임베디드(라즈베리파이, MobileOne S0 ONNX 4.8MB)
  실시간 구현 + 좌/우 2방향만 요구해 저해상도 웹캠에서도 견고(실측 100%).

## 5. 심사 예상 질문 대비

- **"왜 시선으로 숫자를 직접 안 보나?"** → 직접 응시는 10방향 정밀도가
  필요해 저가 카메라에서 불안정하고, 시선 방향 자체가 관찰자에게 힌트가
  된다. 이진 응답은 ±13° 임계로 견고하며(실측 20/20) 관찰돼도 0비트.
- **"2택이면 찍어도 반은 맞지 않나?"** → 라운드가 아니라 PIN이 판정 단위.
  무작위 응답의 PIN 성공률은 1/10⁴로 키패드와 동일.
- **"녹화 공격은?"** → 눈+화면 동시 녹화가 필요하며, 화면에 시야각 필터를
  붙이면 측면 녹화로는 분할을 읽을 수 없다. 잔여 위험은 키패드 이하.

## 6. 구현 파라미터 (검증 완료 값)

- 시선 FSM: 진입 LEFT 16°/RIGHT 12°, 복귀 9°, 중앙값 필터 7, 디바운스 3,
  CENTER 중 baseline EMA 0.03, anti-stick 20프레임.
- 선택 이벤트: 진입 시 1회 펄스 소비, 중앙 복귀 확인 후 다음 입력 허용.
- 3점 개인 캘리브레이션(중앙/좌/우)으로 임계 자동 산출 권장.
